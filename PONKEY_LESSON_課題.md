# PONKEY LESSON 課題リスト

対象: `lesson.html`（レッスン1: つかいかた / レッスン2: つくりかた）
関連: `../ponkey-midi/web/apps/ponkey-guide.html`（開発者向けステータスミラー。あちらが正）

**このリポジトリの `lesson.html` が唯一の正。** 開発用のコピーを ponkey-midi 側に置き直さないこと
（PWA ヘッダとホーム導線がこちらにしかないため、上書きコピーで消える）。

## いまの状態（2026-08-17 公開）

- レッスン1（11ステップ）・レッスン2（6ステップ）とも、通しで自動遷移してホームに戻るところまで動作確認済み。
- 直近の10件の指摘（円柱のパート独立 / ドラムの発音タイミング / 点線リーダー線 / ルーパーの長さ記録 /
  再押下で消えない / Push!! 中の盤面ロック / CLEAR→1ループ→自動遷移 / アドバタイズモードの説明 /
  PC注記のカッコ書き / P5-P7 の音名表示）は実装＋ブラウザで検証済み。
- iPhone 想定でレッスン1が 740px、レッスン2が 696px（画面 812px）。ともに1画面に収まる。
- ハブ（`index.html`）の先頭タイル、`sw.js` の CORE に登録済み。

---

## A. 実機とのズレ（ファーム側の対応が要るもの）

**A-0. 2026-08-18 追記: レッスン3「しくみ」を追加**
Fn サブモード（P1〜P7）/ エフェクト（P5+P6+P7）/ インテリア（P1〜P4 1秒）を 16 ステップで解説。
盤面の G キーと P キーの役割を「いまのモード」で描き分け、HUD に BPM・ループ・スロット・オクターブ・
キー・スケール・グルーブ・音色・音量・エフェクト・SCALE を常時表示する。
状態は本体のキーイベントから推定（`D`）し、v4.5.63+ の `SONG_CMD_DUMP_SLOT 0xFF`（META + 現在スロット）と
BLE MIDI の CC 通知で答え合わせする。**本体が報告しないもの**（パート/マスター音量・エフェクト深さ・静音・
インテリア・テストモード）は推定値のまま HUD に `?` を付けている。実機なしのときは Fn をタップで
押しっぱなし扱い、エフェクト/インテリアのステップは P キーのタップで入退出できる。
未検証: 実機での通し（v4.5.63 以降のファームでのみ META が返る）。

**A-1. 実機のパターン状態が読めない ★最重要**（→ v4.5.63 で部分的に解消。下記追記）
`DBG_CMD_DUMP_STATE` が空スタブ。アプリはキーイベントから状態を推測するしかなく、
取りこぼすと画面と実機がずれたまま戻らない。現状の対策は接続時とレッスン終了時に
BLE MIDI の `CC_RESET (0x42)` を送って両方を空に揃えるだけ。
→ ファームに state dump（全パートのステップ/音符、選択パート、BPM、ループ長、スケール、ルート）を
実装すれば根治する。目安40行。**いま別セッションが `PONKEY_V2_DEBUG.h` を触っているので、
そこに入るかどうかを先に確認する。**
→ **2026-08-16 のファーム v4.5.63 で `SONG_CMD_DUMP_SLOT`（0x5A 0x07 devId slot）が入った。**
  0xFF=現在スロットで META（BPM/スロット/再生/スケール設定/根音/グルーブ/音色×7/中身マスク）と
  現在スロットの中身（V2 形式: ループ長/オクターブ×3/矩形/ドラム16bitマスク/シンセ）が返る。
  レッスン3 は META と矩形/オクターブだけ使っている。レッスン1・2 のパターン同期に使うのは未着手
  （ドラム/シンセの中身を `S.song` に写す変換が要る。ECHO の `parseSlotV2` が流用できる）。

**A-2. 選択中パートも推測値**
`KEY PART_DOWN` から追っているだけ。パートキーのダブルタップ消去（v4.5.39）や
Fn 併用の操作を挟むと実機と食い違う。A-1 が入れば解決。

**A-3. 円柱のパート独立は「表示上の仕様」**
ファームを読む限り実機のソレノイドは選択パートに関係なく、鳴った音すべてで駆動する。
アプリは指示どおり選択中パートの音だけ円柱を上げる。実機と並べると挙動が違って見える。
→ どちらを正とするか未決。実機を合わせるならファーム変更。

**A-4. サブモードに追従できない**
Fn×P1（スロット選択）、Fn×P4（オクターブ→キー→スケール）、Fn×P3（ループ長）に入っても
イベントが飛んでこないので、アプリは何も表示できない。レッスン2のステップ5・6は
「実機ではこう操作する」と文章で説明しているだけで、実機操作をアプリが検知できない。

**A-5. 同時駆動数の制限が未反映**
`MAX_SIMULTANEOUS_SOLENOIDS` をオフライン時のアニメーションに入れていない。
実機では鳴らない音がアプリでは円柱が上がる。

**A-6. BPM / スケール / ルート / ループ長が取得できない**
アプリ側の値で描画している。音名表示（P5-P7）も root=48(C3) 固定・`SCALE_DEFS` 準拠なので、
実機側でオクターブやキーを変えると表示が嘘になる。A-1 に含めて取得したい。

---

## B. アプリ側で片付くもの

**B-1. iOS で読み上げが無音になりうる**
読み上げボタンを廃したので、ユーザー操作を伴わずに `speechSynthesis.speak()` を呼ぶ経路がある。
iOS Safari は初回のユーザージェスチャまで発話をブロックする。
→ 最初のタップ（レッスン選択のタップ）で空発話して unlock する処理を入れる。

**B-2. ルーパーの量子化解像度が実機と一致しているか未確認**
アプリは16ステップ格子に丸めている。実機が同じ解像度で記録しているかは未検証。

**B-3. レッスン終了時の `CC_RESET` は全消去**
ユーザーが自分で作ったパターンも消える。合意済みの挙動だが、画面上に何の予告もない。
→ 終了時に一言出すか、確認を挟むか要判断。

**B-4.** ~~`prog()` / `cur()` の null ガードが未検証~~ → 2026-08-17 に localhost 配信で確認済み（コンソールエラーなし）。

**B-5. デモボタンのラベルがレッスンで違う**
レッスン1は「DEMO」、レッスン2は「きいてみる」。統一するか、意図的に分けるか要判断。

---

## C. 未決・ユーザー確認待ち

- **C-1.** イラストの塗り「B70」を 70% 黒（`#4D4D4D`）と解釈したまま。合っているか未確認。
- **C-2.** レッスン2 のステップ7・8 は未設計（「1〜6まででいい」との指示のため保留）。
- ~~**C-3.** 公開するのか。~~ → 2026-08-17 に `ponkey-web/lesson.html` として公開済み。

---

## 再開時の手順

1. ファーム側の状況を確認する（`git -C ../ponkey-midi log` と最新の `PONKEY_申し送り_*.md`）。
   `DBG_CMD_DUMP_STATE` が実装されたかどうかで A-1〜A-2、A-6 の作業量が変わる。
2. 入っていれば、アプリ側に state 取得→反映を実装。入っていなければ A-3 の方針だけ先に決める。
3. B-1（iOS 読み上げ unlock）は独立して先に潰せる。

## 直し方

`lesson.html` を直接編集する。ここは push = 即公開なので、`index.html` / `sw.js` を
触ったときは `sw.js` の `CACHE` のバージョンも上げること（上げないと古い版が残る）。
