# ponkey-web

PONKEY のエンドユーザー向け公開 Web アプリ。**GitHub Pages でデプロイされる**（push = 公開）。PWA 対応。

## 構成

- `ponkey.js` — **全アプリ共通の BLE クライアント**（2026-08-22 に全アプリの BLE 層をこれ経由に移行済み。
  lesson.html を含め未移行なし）。**正本は ponkey-midi リポジトリの `web/ponkey.js`** — 修正は必ず正本に
  入れてからここへコピーで同期する（直接編集しない）。プロトコル仕様は ponkey-midi の
  `PONKEY_CONTROL_PROTOCOL.md`。
  **パート構成の表もここにある** — `Ponkey.PARTS`（パート番号・名前・色相・MIDI ch・音階テーブル・音域シフト）/
  `Ponkey.KIT_NOTES`（ドラム8キット）/ `Ponkey.keyFromNote(ch,note,kit)`（本体の NoteOn → 鍵番号の逆引き）。
  各アプリが写経して持っていた表はここへ集約したので、ファームが変わったらここだけ直す。
  新旧構成の判別は `hasFeature('PART_REDESIGN')`（FEAT3 bit2 = ファーム v4.6.0+）。
- `index.html` — アプリハブ（PONKEY Web Apps）。各アプリへの入口
- `songbook.html` / `songbook.js` — SONGBOOK（うたのほん。ライトテーマ）。**著作権の切れた曲だけ**を持ち、
  曲を選ぶと PONKEY が弾く。データとエンジンは `songbook.js`、UI は `songbook.html` に分けてある。
  **前提**: PONKEY の16鍵は `NOTE_TABLE_NORMAL` = C3〜D5 の白鍵16本＝全音階のはしごで、黒鍵が無い。
  なので曲は「度数 0-15 ＋ 長さ(16分音符いくつ)」に落として持つ（notation の書き方は `songbook.js` 冒頭）。
  **出口は BLE の生 NoteOn(鍵番号 0-15)** = `requestSolenoidFire` だけ（DRAWING と同じ窓口。パート選択も
  ソングモードも要らない）。内蔵音源で鳴らす分は **ch16 パススルー**（要 v4.5.65+。旧FWで鍵が余計に動くので
  「本体の音」で OFF にできる）。試聴は WebAudio なので**本体が無くても曲を確かめられる**。
  「たかさ」は半音ではなく**度数（白鍵の段数）で上下**する — 半音で動かすと黒鍵に落ちて弾けなくなるため。
  はみ出す音が出たら黙って潰さず画面に件数を出す。
  **MIDI 取り込み**（左下にドロップ）: SMF を読む → 音数と平均の高さで旋律パートを選ぶ → 同時に鳴る音は
  上を採って単旋律化 → 音名ヒストグラムで調を当ててハ長調へ移調 → 残った黒鍵を隣の白鍵へ寄せる →
  収まりのいいオクターブへ折り返す → 16分音符に丸める。**寄せた数・折り返した数は必ず表示する**。
  取り込んだ曲と「みみで たしかめた」印は localStorage（`ponkey_songbook_add_v01` / `_ok_v01`）。
  ★ 内蔵曲の音符は記憶から書き起こしたもので、**耳での検証はユーザーが行う前提**（そのための「たしかめた」）。
  曲を足すときは `SONGS` に1行足すだけ。**`pd` に著作権が切れている根拠を必ず書く**（書けない曲は入れない）。
- `lesson.html` — LESSON（使い方・作り方・しくみの3レッスン。ライトテーマ）。**このファイルが唯一の正**。
  残課題・設計メモは `PONKEY_LESSON_課題.md`。
  レッスン3「しくみ」は Fn サブモード / エフェクト / インテリアの解説で、**本体の状態を推定して映す**
  （本体はモードを報告しないので、キーイベントをファームと同じ規則で追う `D` モデル。
  BPM/スロット/キー/スケール/グルーブ/音色/オクターブ/ループ矩形は `SONG_CMD_DUMP_SLOT 0xFF` の
  META+SLOT_DUMP と BLE MIDI の CC 通知で答え合わせ。v4.5.72+ なら `DBG_EV_MODE_UI` と拡張 META が届くので
  推定をやめて本体の申告を正とし、HUD の `?` も消える。旧FWのみ推定フォールバック）。
  Fn×Pn の割当や音色名はファーム `PONKEY_PT2_v4_5_34.ino` / `PONKEY_V2_DATA_STRUCTURES.h` から写しているので、
  ファームが変わったら `L3` の本文と `VOICE_NAMES` 等の表を追従させる（**現在 v4.6.20 準拠**）。
  v4.6 追従で入れ替えたもの: L1/L2 は「P1=パッド＋長押し録音 / P2-P7=メロディ6パート」の操作モデルへ全面書き直し、
  L3 は Fn+G＝クイックページ（プリセット8/スケール/キー/BPM/音量）と P1+PN＝曲のスロット6つ、
  P7+P2〜P6＝インスタントFXの項を新設、廃止された SCALE 設定・パターン流し込みの2項を削除、
  ステッパ下段＝現パートのオクターブへ。Fn×P1 はページを持たず即 再生/停止。
  **パートのアクセント色は `PART_HUE`（ファームの `PART_BASE_HUE` の写し）から導出**しているので、
  ファームが再配色されたらその配列だけ直せばよい。
  v4.5.69+ の窓口を使用: Push!! ロック中はグリッドを `claim()`、お手本キーは `ledFrame()` で白点滅、
  接続時は `dumpSlot()` で本体パターンを読んで写す（`hasFeature()` でゲート、旧FWは CC_RESET にフォールバック）。
  BLE 層は `ponkey.js` 経由（`modeui`/`songmeta`/`slotdump`/`cc`/`keydown` 等のイベントを購読。
  円柱の動きに使う SOLENOID カテゴリだけ `dbgRaw(0x03,[0x03])` で追加購読している）
- `duet.html` — PONKEY AI Duet（Magenta MusicRNN と交互に弾く）。**v4.6 追従済み**:
  受信 ch → パートは `Ponkey.PARTS` から作る（旧 `ch===0 ならベース` は v4.6 で誤り。
  ベースは ch2）。レイヤー1 の ch も同じパートとして扱う。Tone.js の音色は 4→6 パート分。
  **ドラムのステップ打ち込み（drumPat / CC 0-3 のパート選択 / CC 16-31）は撤去**した —
  v4.6 で本体から「置いて待つ」操作が無くなり、CC 16-31 も対角しか書けなくなったため。
  代わりに**本体で叩かれたキット音（ch9）に反応して鳴らす**（列 `key%4` = 系統）。
  キット番号は `CC_VOICE_SELECT` の part0 で追う。
- `dialogue.html` — DIALOGUE。**v4.6 追従済み**: ch → 音階ラダーと fam(B/N) を `Ponkey.PARTS`
  から作る。fam は「そのパートの白鍵テーブルが低域か」で判定（パート番号で決め打ちしない）。
  ドラムは `NoteOn(鍵番号)` でソレノイドを直接叩く方式なので、キット化の影響を受けない。
- `sequence.html` — SEQUENCE（＋グルーブパネル）。**ファーム v4.6 未対応**（ドラム4パートのグリッドが前提。
  v4.6 で P1 は16音のキットになり、ドラムのステップ編集そのものが廃止された）。
  index のカードはグレーアウト＋バッジ、アプリ内にも赤い注意バナーを出してある。何を出すかから決め直し
- `loops.html` — LOOPS（ループ長。ライトテーマ）。**同じ理由でファーム v4.6 未対応**（グレーアウト済み）
- `invaders.html` — STEP INVADERS（4×4 リズムゲーム。倒す＝演奏。16セルは常にスケール上の音で、
  敵はコード進行のコードトーン上に湧く。スケール/キーは TRANCE と同じ写像方式）
- `groove.html` — GROOVE（ワンボタンでスイング/アクセント。ライトカラー）
- `groove-engine.js` — 解析・提案エンジン（純粋関数、SEQUENCE と GROOVE が共有）。設計は `PONKEY_GROOVE_CONCEPT.md`。
  **v4.6 追従済み**: DUMP のオクターブ欄 3→6（ここを外すと以降のバイトが全部ずれ、実際に壊れていた）。
  ドラムは1パート16音になったが、**列 `key % 4` で キック/スネア/ハット/パーカッションの4系統に束ねる**ので、
  「キックは1拍目・スネアはバックビート・ハットは8分」という従来の見立てがそのまま成立する。
  メロディは 3→6 パート。`f.drum[0-3]` / `f.mel[0-5]` / `f.bass` で引く（数値インデックス直書きは廃止）。
- `sync.html` — SYNC
- `mood.html` — MOOD（スケール／キー／**鍵盤の物理配置**を切り替えて曲の雰囲気を変える。ライトテーマ）。
  肝は「PONKEY のソレノイド16本が下の鍵盤のどの鍵に乗っているか」をアプリに教えること
  （`白鍵だけ` = ファーム既定の `NOTE_TABLE_NORMAL` と同じ C3-D5 白鍵16本 / `半音階` = 起点から16半音）。
  そこから「そのスケールの音を出せる鍵はどれか」を出し、ピアノロールの行を絞る。
  スケールやキーを変えると、既存の音は最寄りのスケール内の鍵へ寄る（＝同じ曲の雰囲気だけが変わる）。
  出口は2つ: 鍵のクリック＝`NoteOn(鍵番号0-15)` を即送信、フレーズ＝`SONG_CMD_LOAD_SLOT`(slot0)＋`SET_SEQ([0])`
  を250msデバウンスで送って本体に鳴らさせる。再生ヘッドは本体の `CC_BAR_SYNC` に乗せる（アプリで数えない）。
  **ドラムは扱わない** — v4.6 の P1 は16キー＝16打楽器のキット（列＝キック/スネア/ハット/パーカッション）で、
  音階の話と噛み合わないため。
  半音階配置のときは本体の内蔵シンセ(SAM2695)だけが白鍵のままズレるので、`CC_SAM_MUTE` で消せるようにしてある。
  **メロディ6パート（P2-P7 = KEYS/LEAD/BASS/CHORD/BELL/VOICE）を扱う**（v4.6.0 の再編に追従済み。
  旧 BASS/LEAD/PAD の3パートで保存した内容は BASS→BASS / LEAD→LEAD / PAD→CHORD へ移して読む）。
  音域はパートの属性（ベース −24 / リード +12 / ベル +24 半音）だが**どれも12の倍数＝音名は同じ**なので、
  「生の鍵盤に合わせる」の1つの値を全パートに入れてよい。
  **ソレノイドが動くのは1パートだけ** — ファームは `solenoidPartMode == p`(= `legacy.currentMode`)の
  パートしか鍵を叩かない。しかも `currentMode` の初期値は 0=P1(ドラム) なので、**指定しないとシンセは鳴るのに
  ソレノイドが1本も動かない**。`CC(パート番号 1-6)` を接続時と切替時に送って解消している
  (本体の P2-P7 を押すのと同じで、本体側の選択パートも変わる)。
  パートの音の ON/OFF は `SONG_CMD_SET_MUTE`(payload=[muteMask][soloMask]、メロディ i のビットは i+1)。
  ミュートしたパートは本体側で丸ごとスキップされるので音もソレノイドも止まる。
  再生中は `CC_BAR_SYNC` に合わせて「そのステップでソレノイドが落ちる鍵」を鍵盤ストリップ上で光らせる。
  **内蔵シンセの音色・音量はアプリから変えられない** — `CC_VOICE_SELECT`(0x30) 等は本体→アプリの
  通知にしか使われておらず、`handleCC` が受け取らない(Program Change も無視)。本体の Fn×P7 / Fn×P6 のみ。
  変えられるのは高さだけで、`LOAD_SLOT` の `[oct×6]` = `octaveOffset`（**v4.6.0 で 3→6 要素**。
  ここを間違えると以降のバイトが全部ずれる）。
  **名前はオクターブだが単位は半音**、実レンジは `FN_OCTAVE_MIN/MAX` の **-4〜+11**
  (`applyOctaveOffset` のコメントにある「-8〜+7」は古い)。接続時にファームが 0 にリセットするので
  アプリが毎回入れ直す前提。「生の鍵盤に合わせる」で最適値を出す —
  白鍵配置は差が全鍵一定(起点 - C3)なのでぴったり合うが、**半音階配置は合わせきれない**
  (内蔵シンセの白鍵16本 = 約26半音幅 に対し配置は16半音幅で、音程の幅そのものが違う)。残差は画面に出す。
- `scale.html` — SCALE（メロディ6パート(P2-P7)の16キーが弾く音階を切り替える。1画面・ライトテーマ。MOOD の軽量版ではなく別物）。
  **ファーム側の入口が要る**（PT2 v4.5.64+）: `CC_SYNTH_SCALE 0x37`（0-7・8=半音階 / 0x7F=白鍵）と `CC_KEY 0x38`（0-11）。
  どちらも双方向で接続直後の State Sync に含まれるので、**アプリは本体を正**とし、接続時に届いた値へ画面を合わせる
  （本体側に電源を切っても残る設定なので、アプリの記憶で上書きしない）。4秒以内に届かなければ「古いファーム」と表示。
  スケールの並び・音程はファーム `SCALE_DEFS` と同じ順で持ち、16キーの音名は `scaleNoteForKey()` と同じ算出
  （根音 C3+key から度数を下に敷き詰め。BASS は −24）。試聴は WebAudio のみで、本体の音は本体の16キーを押して聴く
  （BLE NoteOn 0-15 はソレノイドしか動かさず内蔵音源は鳴らないため）。P1-P3 の SCALE モードとは無関係（キーの根音だけ共用）。
  **鼻歌で探す**（🎤）: マイク→自己相関ピッチ検出→半音未満の全体ズレを補正→音高クラスのヒストグラム→
  8スケール×12キー総当たり（score=カバー率+主音ボーナス−音数ペナルティ）で上位3件を提案、タップで適用。
  音感がなくても成立するのは「ハミングはキーがズレても音程の間隔は保たれる」ため（スケールの形は当たる。
  キーは歌った高さから推定＝ひとり演奏なら元のキーである必要がない）。原曲と合わせたい場合には使えないことは把握済み。
- `drawing.html` — DRAWING（画面をなぞると PONKEY が動いて光る。ダーク・1画面キャンバス）。
  画面全体を 4×4 に割り、マス = 鍵番号 0-15（左上=0）。ポインタが**マスに入るたびに `NoteOn(鍵番号)`** を送る
  （BLE NoteOn は `requestSolenoidFire` だけ = ソレノイド＋LED。録音も音源も動かない副作用なし。velocity は無視される）。
  「連打」ON なら同じマスをなぞり続けても 140ms ごとに送る。線の色 = 7パート色（色相はファーム `PART_BASE_HUE` と同じ）で、
  **色を選ぶと `CC(パート番号 0-6)` を送って本体 LED の色も変える**（P1〜P7 を押すのと同じなので本体の選択パートも変わる）。
  キー 1〜7 でも選べる。本体の鍵を押すと NoteOn が返るので、そのマスに今の色で点を打つ（逆引きは INVADERS と同じ表）。
  **音（要ファーム PT2 v4.5.65+）**: BLE の **ch16（0x9F/0x8F/0xBF/0xCF）** に送ったものは本体が内蔵音源 SAM2695 の ch16 へ素通しする
  （`BLE_LIVE_CHANNEL`、本体の状態には触らない）。叩くのは従来どおり ch1 の NoteOn を別送。マス→音高はメジャーペンタ C3 起点、
  色 P1〜P7 ↔ パッド系 GM 音色（`PARTS[].voice`）、接続時に PC/CC7/CC91=127/CC93/CC72=110 を送る。マスを離れて 650ms 後に NoteOff
  （隣どうしが重なって和音になる）。**旧ファームだと ch16 の NoteOn が普通の NoteOn 扱いになり音高に当たる鍵が余計に動く**ので、
  「音」ボタンで OFF にできる（tooltip に注意書き）。
  **本体 LED の絵（要ファーム PT2 v4.5.66+）**: 描いたマスは `CC_LED_PAINT 0x39`（val = key<<3 | level 0-7）で
  本体 LED も現パート色で光り続ける（ソレノイドと独立の床上げオーバーレイ）。「消えていく」ON なら 420ms ごとに
  level を下げて送る = 本体の絵も一緒に消えていく。クリアで全消去、切断でファーム側も自動全消去。旧ファームは未知 CC を無視するだけ。
  グリッドは**ウィンドウに収まる最大の正方形**（#board）で、マスは本体と同じ正方形。
- `trance.html` — TRANCE / Translator（PONKEY → Logic・Ableton の MIDI ブリッジ。最大4台）。
  **ファーム v4.6 のパート構成が正**: PONKEY ch0-5 = P2-P7 メロディ（レイヤー1 は ch6,7,8,10,11,12 で
  同じパートへ寄せる）/ ch9 = ドラムキット。出力は **Logic の chN = PONKEY の PN** に整理してある
  （ch2-7 = P2-P7 / ch1 = 旋律化したドラム / ch10 = 素のドラム GM 互換）。
  ドラムの旋律化は「ステップ位置 → 音高」ではなく**「叩いたキー(打楽器) → 音高」**（v4.6 で
  ドラムがパッドになったため）。キット8種は `CC_VOICE_SELECT` の part0 で追う（音の中身が変わるので、
  これが無いと ch9 の逆引きが外れる）。
  **アプリ側のドラムステップシーケンサは撤去済み** — ファームが「置いて待つ」をやめたので、
  アプリだけ旧モデルを持つと本体と食い違う。クロック同期（BPM 配信・PLAY/STOP・位相合わせ）は残す。
  Mute/Solo の index は 0 = P1 ドラム / 1-6 = P2-P7。
  **STEP モード (v2.1)**: 「アプリによって本体の動きが大胆に変わる」を**ファーム無改修で**実装した例。
  `claim({grid:true})` で本体のグリッド処理を止め（`DBG_EV_KEY_*` は届き続ける）、`ledFrame()` で
  選択中サウンドの16ステップを描き、押されたキーをステップとして解釈し、結果を `loadSlot()` で
  本体へ書く（鳴らすのは本体のエンジン）。**16打楽器 × 16ステップ**（v4.6 でドラムの1バイトが
  `(サウンド<<4)|ステップ` になったため表現できる）。入る時に `dumpSlot()` して**メロディは保持**する。
  ※ SONG_CMD はデバッグサービス経由なので、接続は `debug:true`。ただし SONG/MODE の
  ログ通知は接続直後に切って、4台つないでも BLE を埋めないようにしてある。
  claim は Fn 2秒長押しと切断でファーム側から必ず解除される（`claim` イベントで画面も戻す）。
- `echo.html` — ECHO（その日の演奏を本体から読み出して残す日記＋共有リンク）。
  取り込みは `SONG_CMD_DUMP_SLOT`(0x07, payload 0xFE=中身のある全スロット)。ソングモード不要・副作用なし。
  応答は `DBG_EV_SONG_META` → `DBG_EV_SONG_SLOT_DUMP`（分割・要組み立て）で、スロットは **V2 形式(先頭 0xF2)**。
  **v4.6 でスロットの形が変わった**（オクターブ欄 3→6要素／ドラムは1パート＝16音のキット／メロディ6パート）。
  取り込み時に `hasFeature('PART_REDESIGN')` を見て記録へ `parts2` の印を付け、ミニ図・書き戻し・
  共有リンク（magic 0xE1=旧 / 0xE2=v4.6）を出し分ける。**旧ファームで取った過去の記録とリンクは読めるまま**。
  **書き戻しの欠落**: ファームの `LOAD_SLOT`(`parseSlotPayload`) は V1 形式しか受けない。
  v4.6 の記録はドラムのマスクを全ビット戻せる（V1 の1バイトが `(サウンド<<4)|ステップ` に意味変更されたため）。
  旧記録は①ドラムの非対角ビットと矩形設定が落ちる。②シンセの重ね録りレイヤー（`raw` の bit7）は共通で落ちる。
  保存データ側は V2 のまま無傷なので、ファームが V2 受信に対応すれば後から完全復元できる。落ちた件数はログに出す。
  **バックアップは GitHub Gist**（非公開＝secret gist。ただし URL を知る人は見られる＝完全な非公開ではない）。
  トークンは記録本体とは**別の localStorage キー** `ponkey_echo_gist_v01` に置く（`ponkey_echo_v01` と混ぜると
  「書き出し」の JSON にトークンが載ってしまうため）。同期は id で突き合わせる和集合マージだが、
  **削除は墓標 `store.deleted` が必須**（無いと消した記録が次の同期で Gist から蘇る）。
  Gist は 1MB 超で `content` が切り詰められるので `truncated` なら `raw_url` から取り直す
- `ponkey-sound-guide.html` — 音の世界観・リスニングガイド
- `manifest.json` / `sw.js` / `icon-*.png` — PWA（インストール・オフライン）
- `PONKEY_SESSION_CONCEPT.md` — 設計思想ドキュメント。**なぜその挙動なのかはここに書いてある**

## 作業ルール

- アプリを追加したら **`index.html` のハブに導線を追加**し、`sw.js` のキャッシュ対象も更新する。
  そのとき **`sw.js` の `CACHE` バージョンも上げる**（上げないと利用者に古い版が残る）。
  全アプリにホーム導線がある設計なので、新規ページも同じ導線を持たせる。
- **エンドユーザーが触るページはここが正。** `../ponkey-midi/web/apps/` は開発・デバッグ専用。
  向こうで作ったものを公開するときは「移す」（コピーを残さない）。二重管理になると、
  こちらにしかない PWA ヘッダとホーム導線が上書きで消える。
- 挙動の「なぜ」を変える変更をする前に `PONKEY_SESSION_CONCEPT.md` を読む。
  ドキュメントと実装が食い違ったら、勝手にどちらかへ寄せずユーザーに確認する。
- **push すると即公開される。** ただしユーザーの方針として、明示的に「push しないで」と言われない限り、
  作業が終わったら確認を待たずコミット＆push してよい(2026-08-18 指示)。検証は省かないこと。
  **他セッションの未コミット変更は巻き込まない** — `git add -A` は使わず、触ったファイルだけを add する。
- ファーム側との契約（BLE コマンド等）は `../ponkey-midi/ponkey-firmware/` が正。
  こちらで勝手に仕様を決めない。
- `sync.html` はこのリポジトリのもの。別リポジトリ `../ponkey-sync/` とは**別物**。
